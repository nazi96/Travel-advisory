// World Travel Advisory - lightweight client-side enhancements
document.addEventListener("DOMContentLoaded", () => {
    // Auto-focus the country selector if present
    const sel = document.getElementById("country");
    if (sel) sel.focus();
});
